#include "main.h"

/**
 * _isupper - check if uppercase lettters
 * @c: char to check
 * Return: if uppercase return 1, else return 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= '2')
		return (1);

	return (0);
}
